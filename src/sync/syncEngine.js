// Push/pull sync between the local Dexie store (src/db/localDb.js) and
// Firestore, plus conflict handling for append-only bills/payments
// (architecture.md §2 "Offline behavior", §5, §7). The riskiest bet in
// this project — see phases.md's build-order note, which calls for
// proving this by the end of Phase 4, not leaving it to Phase 11.
// Left as a stub here so the folder structure from architecture.md §3
// is in place from Phase 0.
